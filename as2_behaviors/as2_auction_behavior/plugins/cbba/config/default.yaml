auction_behavior:
  ros__parameters:
    bundle_size_limit: 0            # 0 = unlimited (compete for all items); positive = cap per drone
    retransmit_timeout_s: 2.0       # seconds before retransmitting stale bids
    # item_plugin_type: set this to the item plugin to use, e.g. "coordinate_item"
    # state_component: list of state topics needed by the chosen item plugin
